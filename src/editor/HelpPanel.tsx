// src/editor/HelpPanel.tsx
// Свёрнутая по умолчанию памятка терминов конфигуратора — на случай, если наведение на «?»
// рядом с полем не так просто найти с первого раза.
import { STATION_COLORS, STATION_HINTS, STATION_LABELS, StationTypes } from '../domain/types';

export function HelpPanel() {
  return (
    <details className="help-block">
      <summary>❔ Как читать параметры станции</summary>
      <dl>
        <dt>Как соединить станции</dt>
        <dd>
          Потяните за голубой кружок на правом краю станции и отпустите на другой станции — появится связь.
          Либо кнопка «Связать со станцией →» в панели параметров, затем клик по нужной станции на плане.
        </dd>

        <dt>Людей (одновременно)</dt>
        <dd>Сколько сотрудников/постов на станции работают параллельно — каждый обрабатывает одну партию за раз.</dd>

        <dt>Время обслуживания</dt>
        <dd>
          Сколько занимает ОДИН цикл обработки партии. Задаётся не одним числом, а распределением — реальные
          процессы почти никогда не занимают строго одинаковое время. Разверните подсказку под самим полем —
          там объяснение каждого вида, включая «треугольное».
        </dd>

        <dt>Размер партии (batch)</dt>
        <dd>Сколько единиц нужно накопить в очереди, чтобы запустить цикл — например, сколько паллет помещается в 1 грузовик.</dd>

        <dt>Лимит очереди</dt>
        <dd>Максимум единиц, ожидающих перед станцией одновременно. Сверх лимита — единица считается потерянной.</dd>

        <dt>Условие маршрутизации (на связи между станциями)</dt>
        <dd>Сущность идёт по связи, только если совпадает со всеми заданными полями (поток/тип груза/назначение/состояние).</dd>
      </dl>
      <div className="help-legend-title">Типы станций</div>
      <ul className="help-legend">
        {StationTypes.map((t) => (
          <li key={t}>
            <span className="palette-dot" style={{ background: STATION_COLORS[t] }} />
            <div>
              <div className="help-legend-name">{STATION_LABELS[t]}</div>
              <div className="help-legend-desc">{STATION_HINTS[t]}</div>
            </div>
          </li>
        ))}
      </ul>
    </details>
  );
}
